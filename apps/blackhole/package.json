{
  "name": "@repo/blackhole",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "wrangler dev --env-file ../../.env.local",
    "deploy": "wrangler deploy --env-file ../../.env.local",
    "logs": "wrangler tail --env-file ../../.env.local"
  }
}
